import * as $ from 'gotcha';
import {$} from 'still-fails';
import {jQuery as nope} from 'some-module';
import {foo as jQuery} from 'expect-failure';

let x = $('#foo');
        ~          [Do not use global jQuery.]

let y = jQuery('#foo');
        ~~~~~~          [Do not use global jQuery.]
